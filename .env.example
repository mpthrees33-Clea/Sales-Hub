# Shared rep password for the login gate. Mirrors the brochure generator's
# SHARED_PASSWORD. Defaults to "trinity" if unset. To override in production,
# add VITE_SHARED_PASSWORD as a repo secret (it is wired into deploy.yml).
VITE_SHARED_PASSWORD="trinity"
